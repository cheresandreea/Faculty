#include "UI.h"
#include "Test.h"
#include <iostream>
#include <crtdbg.h>

int main()
{
	//vector<Dog> adoptedDogs;
	//Repo repo = Repo();
	//Service service = Service(repo, adoptedDogs);
	//UI ui = UI(service);
	//ui.start();
	//_CrtDumpMemoryLeaks();
	runTests();

	return 0;
}